using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStatSOManager : MonoBehaviour
{
    /*[SerializeField] public List<Stats> statsList;




    private const string statsPath = "Assets/_Prefabs/_Characters/Stats/";

    private void OnApplicationQuit()
    {
        statsList = HelperFunctions.GetScriptableObjects<Stats>(statsPath);

        foreach (var stat in statsList)
        {
            stat.ResetPerks();
        }
    }*/
}
