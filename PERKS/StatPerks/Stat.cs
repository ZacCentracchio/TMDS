public enum Stat
{
    maxHealth, 
    maxStamina,
    moveSpeed, 
    dashSpeed, 
    bulletSpeed,
    staminaRechargeSpeed,
    parryLength,
    bulletDamage, 
    meleeDamage,
    bulletRange,
    dashTime,
    dashCooldown,
}